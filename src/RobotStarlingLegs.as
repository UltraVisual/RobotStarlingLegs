package {

import flash.display.Sprite;
import flash.text.TextField;

public class RobotStarlingLegs extends Sprite {
    public function RobotStarlingLegs() {
        var textField:TextField = new TextField();
        textField.text = "Hello, World";
        addChild(textField);
    }
}
}
